version: "3"

services:
  sqs:
    build:
      context: .
      dockerfile: Dockerfile-elastic-mq.yaml
    ports:
      - "127.0.0.1:9324:9324"

  toxicproxy:
    image: shopify/toxiproxy:latest
    network_mode: "host"